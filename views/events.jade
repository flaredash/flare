extends layout

block body
  script.
    var token = ("#{user.customData.token}")  // debug
    var deviceID = ("#{user.customData.deviceID}") // debug
    var device0ID = ("#{user.customData.deviceID}")
  .dashboard
    .row
      .col-lg-12
        .jumbotron        
          //Start Connection Button 
          button(onclick='start()') Connect
          .container
            .row
              #box1.col-md-6
                h4#title1 Current Color
                span#colorIDSpan
                br
                span#colorSpan
                br
                span#colorTimeSpan
              #box2.col-md-6
                h4#title2 Current Score
                span#scoreIDSpan
                br
                span#scoreSpan
                br
                span#scoreTimeSpan
    #footer
      h6 This is an open source project. Fork it on github <a href="https://github.com/flaredash/flare">here</a>.